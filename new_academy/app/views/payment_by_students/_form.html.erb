<%= form_with(model: payment_by_student) do |form| %>
  <% if payment_by_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_by_student.errors.count, "error") %> prohibited this payment_by_student from being saved:</h2>

      <ul>
        <% payment_by_student.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="field">
    <%= form.label :payment_id %>
    <%= form.text_field :payment_id %>
  </div>

  <div class="field">
    <%= form.label :amount_usd %>
    <%= form.text_field :amount_usd %>
  </div>

  <div class="field">
    <%= form.label :amount_bs %>
    <%= form.text_field :amount_bs %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
